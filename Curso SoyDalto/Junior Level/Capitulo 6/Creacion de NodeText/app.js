let caja = document.querySelector('.queen')
let boton = document.getElementById('btnCreate')
let titulos = document.getElementsByTagName('h2')

let textNodeMundo = document.createTextNode('mundo');

/* Este metodo permite crear nodos de tipo TEXT, puede servir para añadirlos a un nodo padre o para escapar 
caracteres. */

