let elemento = document.querySelector('.pero')

console.log(elemento.hasAttribute('class'));

/* El metodo hasAttribute() permite averiguar si un elemento tiene un atributo especificado como parametro. Se retorna
true en caso de existir y false en caso de que no. */

/* El metodo hasAttributes() permite averiguar si un elemento tiene un atributos, no se requiere de parametros. */

