let elemento = document.getElementById('container')

console.log(elemento.children);

// Retorna un HTMLCollection con todos los elements node que sean hijos del elemento al cual llama a la propiedad.