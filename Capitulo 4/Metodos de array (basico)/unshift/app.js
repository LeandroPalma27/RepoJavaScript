let arrays = [2, 3, 4, 5, 6, 7, 8, 9, 10];
console.log(arrays);
arrays.unshift(1);

for (array of arrays) {
console.log(array);
}

// Explicacion: Este metodo es similar al metodo push, con la diferencia de que este añade al inicio
//              y no al final.